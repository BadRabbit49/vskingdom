namespace VSKingdom.Constants {
	public static class GlobalEnums {
		public enum EnlistedStatus { CIVILIAN, ENLISTED, DESERTER }
		public enum Specialization { EMPTY, KNIGHT, ARCHER, MEDIC, BANDIT }
		public enum CurrentCommand { WANDER, FOLLOW, PATROL, RETURN }
		public enum KingGovernance { MONARCHY, DICTATOR, REPUBLIC }
		public enum KingPriviliges { SUBJECT, CITIZEN, SOLDIER, ROYALTY, AUTARCH }
	}
}