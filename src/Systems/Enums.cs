namespace VSKingdom {
	public enum EnlistedStatus { CIVILIAN, ENLISTED, DESERTER }
	public enum Specialization { EMPTY, MELEE, RANGE, BANNR, MEDIC }
	public enum CurrentCommand { WANDER, NOMOVE, FOLLOW, RETURN }
	public enum Governance { MONARCHY, DICTATOR, REPUBLIC }
	public enum Priviliges { SUBJECT, CITIZEN, SOLDIER, ROYALTY, AUTARCH }
}
